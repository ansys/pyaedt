name: Weekly Tests Updating Testmon Caches

on:
  workflow_dispatch:
  schedule:  # UTC at 2pm on Saturday
    - cron:  '0 14 * * 6'

env:
  ANSYSLMD_LICENSE_FILE: ${{ format('1055@{0}', secrets.LICENSE_SERVER) }}
  MAIN_PYTHON_VERSION: '3.10'
  ON_CI: True
  PYAEDT_LOCAL_SETTINGS_PATH: 'tests/pyaedt_settings.yaml'
  PYTEST_ARGUMENTS: -vvv --color=yes -ra --durations=25 --maxfail=10 --junitxml=junit/test-results.xml --testmon
  PYTEST_ARGUMENTS_NON_TESTMON: -vvv --color=yes -ra --durations=25 --maxfail=10 --junitxml=junit/test-results.xml
  TESTMON_DATAFILE: '.testmondata'
  TESTMON_CACHE_KEY_SOLVERS_WIN: 'testmondata-solvers-win'
  TESTMON_CACHE_KEY_SOLVERS_LINUX: 'testmondata-solvers-linux'
  TESTMON_CACHE_KEY_GENERAL_WIN: 'testmondata-general-win'
  TESTMON_CACHE_KEY_GENERAL_LINUX: 'testmondata-general-linux'
  TESTMON_CACHE_KEY_VISUALIZATION_WIN: 'testmondata-visualization-win'
  TESTMON_CACHE_KEY_VISUALIZATION_LINUX: 'testmondata-visualization-linux'
  TESTMON_CACHE_KEY_ICEPAK_WIN: 'testmondata-icepak-win'
  TESTMON_CACHE_KEY_ICEPAK_LINUX: 'testmondata-icepak-linux'
  TESTMON_CACHE_KEY_LAYOUT_WIN: 'testmondata-layout-win'
  TESTMON_CACHE_KEY_LAYOUT_LINUX: 'testmondata-layout-linux'
  TESTMON_CACHE_KEY_FILTER_WIN: 'testmondata-filter-win'
  TESTMON_CACHE_KEY_EMIT_WIN: 'testmondata-emit-win'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Disable all default permissions at workflow level for security (least privilege principle)
# Individual jobs will explicitly request only the permissions they need
permissions: {}

jobs:

  unit-tests:
    name: Unit tests
    runs-on: ubuntu-latest
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Install Xvfb
        shell: bash
        run: sudo apt-get install -y xvfb

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}

      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all

      - name: Run unit tests
        env:
          PYTEST_ARGUMENTS_NON_TESTMON: ${{ env.PYTEST_ARGUMENTS_NON_TESTMON }}
        run: |
          xvfb-run pytest ${PYTEST_ARGUMENTS_NON_TESTMON} tests/unit


  integration-tests:
    name: Integration tests
    runs-on: ubuntu-latest
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
    steps:
      - name: Checkout repository
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}

      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all

      - name: Run integration tests
        env:
          PYTEST_ARGUMENTS_NON_TESTMON: ${{ env.PYTEST_ARGUMENTS_NON_TESTMON }}
        run: |
          pytest ${PYTEST_ARGUMENTS_NON_TESTMON} tests/integration

  system-tests-solvers-windows:
    name: Test solvers (windows)
    runs-on: [ self-hosted, Windows, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: powershell
        run: |
          Get-Process | Where-Object {
            $_.Path -like "*ansys inc*" -or $_.Path -like "*ansysem*"
          } | ForEach-Object {
            Write-Output "Killing $($_.Name) (PID: $($_.Id))"
            Stop-Process -Id $_.Id -Force
          }

      - name: Run tests marked with 'solvers'
        env:
          PYTHONMALLOC: malloc
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          $args = $env:PYTEST_ARGUMENTS -split ' '
          pytest @args --timeout=600 tests/system/solvers

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_SOLVERS_WIN }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_SOLVERS_WIN }}-main-${{ github.sha }}

  system-tests-solvers-linux:
    name: Test solvers (linux)
    runs-on: [ self-hosted, Linux, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
      ANSYSEM_ROOT261: '/usr/ansys_inc/v261/AnsysEM'
      ANS_NODEPCHECK: '1'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: bash
        run: |
          for pid in $(ps -eo pid,comm,args | grep -iE "ansys.inc|ansysem" | grep -v grep | awk '{print $1}'); do
            echo "Killing PID $pid"
            kill -9 "$pid"
          done

      - name: Run tests marked with 'solvers'
        env:
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          pytest ${PYTEST_ARGUMENTS} --timeout=600 tests/system/solvers

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_SOLVERS_LINUX }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_SOLVERS_LINUX }}-main-${{ github.sha }}

  system-tests-general-windows:
    name: Test general (windows)
    runs-on: [ self-hosted, Windows, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Set up headless display
        uses: pyvista/setup-headless-display-action@5bc8de3bc71fcda7a96439571287a554901541a0 # v4.3

      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: powershell
        run: |
          Get-Process | Where-Object {
            $_.Path -like "*ansys inc*" -or $_.Path -like "*ansysem*"
          } | ForEach-Object {
            Write-Output "Killing $($_.Name) (PID: $($_.Id))"
            Stop-Process -Id $_.Id -Force
          }

      - name: Run tests marked with 'general'
        env:
          PYTHONMALLOC: malloc
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          $args = $env:PYTEST_ARGUMENTS -split ' '
          pytest @args -n 4 --dist loadfile --timeout=600 tests/system/general

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_GENERAL_WIN }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_GENERAL_WIN }}-main-${{ github.sha }}

  system-tests-general-linux:
    name: Test general (linux)
    runs-on: [ self-hosted, Linux, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
      ANSYSEM_ROOT261: '/usr/ansys_inc/v261/AnsysEM'
      ANS_NODEPCHECK: '1'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Set up headless display
        uses: pyvista/setup-headless-display-action@5bc8de3bc71fcda7a96439571287a554901541a0 # v4.3

      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: bash
        run: |
          for pid in $(ps -eo pid,comm,args | grep -iE "ansys.inc|ansysem" | grep -v grep | awk '{print $1}'); do
            echo "Killing PID $pid"
            kill -9 "$pid"
          done

      - name: Run tests marked with 'general'
        run: |
          pytest ${PYTEST_ARGUMENTS} -n 4 --dist loadfile --timeout=600 tests/system/general

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_GENERAL_LINUX }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_GENERAL_LINUX }}-main-${{ github.sha }}

  system-tests-visualization-windows:
    name: Test visualization (windows)
    runs-on: [ self-hosted, Windows, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
      MPLBACKEND: 'Agg'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Set up headless display
        uses: pyvista/setup-headless-display-action@5bc8de3bc71fcda7a96439571287a554901541a0 # v4.3

      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: powershell
        run: |
          Get-Process | Where-Object {
            $_.Path -like "*ansys inc*" -or $_.Path -like "*ansysem*"
          } | ForEach-Object {
            Write-Output "Killing $($_.Name) (PID: $($_.Id))"
            Stop-Process -Id $_.Id -Force
          }

      - name: Run tests marked with 'visualization'
        env:
          PYTHONMALLOC: malloc
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          $args = $env:PYTEST_ARGUMENTS -split ' '
          pytest @args -n 4 --dist loadfile --timeout=600 tests/system/visualization -x

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_VISUALIZATION_WIN }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_VISUALIZATION_WIN }}-main-${{ github.sha }}

  system-tests-visualization-linux:
    name: Test visualization (linux)
    runs-on: [ self-hosted, Linux, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
      ANSYSEM_ROOT261: '/usr/ansys_inc/v261/AnsysEM'
      ANS_NODEPCHECK: '1'
      MPLBACKEND: 'Agg'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Set up headless display
        uses: pyvista/setup-headless-display-action@5bc8de3bc71fcda7a96439571287a554901541a0 # v4.3

      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: bash
        run: |
          for pid in $(ps -eo pid,comm,args | grep -iE "ansys.inc|ansysem" | grep -v grep | awk '{print $1}'); do
            echo "Killing PID $pid"
            kill -9 "$pid"
          done

      - name: Run tests marked with 'visualization'
        run: |
          pytest ${PYTEST_ARGUMENTS} -n 4 --dist loadfile --timeout=300 tests/system/visualization -x

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_VISUALIZATION_LINUX }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_VISUALIZATION_LINUX }}-main-${{ github.sha }}

  system-tests-icepak-windows:
    name: Test icepak (windows)
    runs-on: [ self-hosted, Windows, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: powershell
        run: |
          Get-Process | Where-Object {
            $_.Path -like "*ansys inc*" -or $_.Path -like "*ansysem*"
          } | ForEach-Object {
            Write-Output "Killing $($_.Name) (PID: $($_.Id))"
            Stop-Process -Id $_.Id -Force
          }

      - name: Run tests marked with 'icepak'
        env:
          PYTHONMALLOC: malloc
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          $args = $env:PYTEST_ARGUMENTS -split ' '
          pytest @args --timeout=600 tests/system/icepak

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_ICEPAK_WIN }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_ICEPAK_WIN }}-main-${{ github.sha }}

  system-tests-icepak-linux:
    name: Test icepak (linux)
    runs-on: [ self-hosted, Linux, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
      ANSYSEM_ROOT261: '/usr/ansys_inc/v261/AnsysEM'
      ANS_NODEPCHECK: '1'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: bash
        run: |
          for pid in $(ps -eo pid,comm,args | grep -iE "ansys.inc|ansysem" | grep -v grep | awk '{print $1}'); do
            echo "Killing PID $pid"
            kill -9 "$pid"
          done

      - name: Run tests marked with 'icepak'
        env:
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          pytest ${PYTEST_ARGUMENTS} --timeout=600 tests/system/icepak

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_ICEPAK_LINUX }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_ICEPAK_LINUX }}-main-${{ github.sha }}

  system-tests-layout-windows:
    name: Test layout (windows)
    runs-on: [ self-hosted, Windows, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all

      - name: Remove Ansys processes (if any)
        shell: powershell
        run: |
          Get-Process | Where-Object {
            $_.Path -like "*ansys inc*" -or $_.Path -like "*ansysem*"
          } | ForEach-Object {
            Write-Output "Killing $($_.Name) (PID: $($_.Id))"
            Stop-Process -Id $_.Id -Force
          }

      - name: Run tests marked with 'layout'
        env:
          PYTHONMALLOC: malloc
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          $args = $env:PYTEST_ARGUMENTS -split ' '
          pytest @args --timeout=600 tests/system/layout

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_LAYOUT_WIN }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_LAYOUT_WIN }}-main-${{ github.sha }}

  system-tests-layout-linux:
    name: Test layout (linux)
    runs-on: [ self-hosted, Linux, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
      ANSYSEM_ROOT261: '/usr/ansys_inc/v261/AnsysEM'
      ANS_NODEPCHECK: '1'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: bash
        run: |
          for pid in $(ps -eo pid,comm,args | grep -iE "ansys.inc|ansysem" | grep -v grep | awk '{print $1}'); do
            echo "Killing PID $pid"
            kill -9 "$pid"
          done

      - name: Run tests marked with 'layout'
        env:
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          pytest ${PYTEST_ARGUMENTS} --timeout=600 tests/system/layout

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_LAYOUT_LINUX }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_LAYOUT_LINUX }}-main-${{ github.sha }}

  system-tests-extensions-windows:
    name: Test extensions (windows)
    runs-on: [ self-hosted, Windows, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Set up headless display
        uses: pyvista/setup-headless-display-action@5bc8de3bc71fcda7a96439571287a554901541a0 # v4.3

      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: powershell
        run: |
          Get-Process | Where-Object {
            $_.Path -like "*ansys inc*" -or $_.Path -like "*ansysem*"
          } | ForEach-Object {
            Write-Output "Killing $($_.Name) (PID: $($_.Id))"
            Stop-Process -Id $_.Id -Force
          }

      - name: Fix TCL_LIBRARY for tkinter on Windows
        id: fix-tcl
        shell: powershell
        run: |
          # NOTE: In a venv, sys.prefix points to the venv and since Tcl lives in sys.base_prefix (the real install).
          # To ensure we locate the correct Tcl directory, we check both sys.prefix and sys.base_prefix.
          # NOTE: We also try to leverage actions/setup-python's environment variable to locate the Python installation
          # and find the Tcl directory. See https://github.com/actions/setup-python/blob/main/src/find-python.ts
          $prefixes = @()
          if ($env:pythonLocation) { $prefixes += $env:pythonLocation }
          $prefixes += (python -c "import sys; print(sys.base_prefix)").Trim()
          $prefixes += (python -c "import sys; print(sys.prefix)").Trim()

          $tclDir = $null
          foreach ($prefix in $prefixes) {
            $tclDir = Get-ChildItem -Path "$prefix\tcl" -Filter "tcl*" -Directory -ErrorAction SilentlyContinue |
              Where-Object { Test-Path (Join-Path $_.FullName "init.tcl") } |
              Sort-Object Name -Descending | Select-Object -First 1
            if ($tclDir) { break }
          }

          if ($tclDir) {
            "tcl_library=$($tclDir.FullName)" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
            Write-Output "TCL_LIBRARY set to $($tclDir.FullName)"
          } else {
            Write-Warning "Could not locate a valid tcl directory in any of: $($prefixes -join ', ')"
          }

      - name: Run tests marked with 'extensions'
        env:
          PYTHONMALLOC: malloc
          PYTEST_ARGUMENTS_NON_TESTMON: ${{ env.PYTEST_ARGUMENTS_NON_TESTMON }}
          TCL_LIBRARY: ${{ steps.fix-tcl.outputs.tcl_library }}
        run: |
          $args = $env:PYTEST_ARGUMENTS_NON_TESTMON -split ' '
          pytest @args --timeout=600 tests/system/extensions

  system-tests-extensions-linux:
    name: Test extensions (linux)
    runs-on: [ self-hosted, Linux, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
      ANSYSEM_ROOT261: '/usr/ansys_inc/v261/AnsysEM'
      ANS_NODEPCHECK: '1'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Set up headless display
        uses: pyvista/setup-headless-display-action@5bc8de3bc71fcda7a96439571287a554901541a0 # v4.3

      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all

      - name: Remove Ansys processes (if any)
        shell: bash
        run: |
          for pid in $(ps -eo pid,comm,args | grep -iE "ansys.inc|ansysem" | grep -v grep | awk '{print $1}'); do
            echo "Killing PID $pid"
            kill -9 "$pid"
          done

      - name: Run tests marked with 'extensions'
        run: |
          pytest ${PYTEST_ARGUMENTS_NON_TESTMON} --timeout=600 tests/system/extensions

  system-tests-filter-windows:
    name: Test filter solutions (windows)
    runs-on: [ self-hosted, Windows, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: powershell
        run: |
          Get-Process | Where-Object {
            $_.Path -like "*ansys inc*" -or $_.Path -like "*ansysem*"
          } | ForEach-Object {
            Write-Output "Killing $($_.Name) (PID: $($_.Id))"
            Stop-Process -Id $_.Id -Force
          }

      - name: Run tests marked with 'filter_solutions'
        env:
          PYTHONMALLOC: malloc
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          $args = $env:PYTEST_ARGUMENTS -split ' '
          pytest @args --timeout=600 tests/system/filter_solutions

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_FILTER_WIN }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_FILTER_WIN }}-main-${{ github.sha }}

  system-tests-emit-windows:
    name: Test EMIT (windows)
    runs-on: [ self-hosted, Windows, pyaedt ]
    permissions:
      contents: write # Needed to save cache
    env:
      TESTS_VERSION: '3.10'
    steps:
      - name: Install Git and checkout project
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.3.0
        with:
          python-version: ${{ env.TESTS_VERSION }}

      - name: Set up uv
        uses: astral-sh/setup-uv@fac544c07dec837d0ccb6301d7b5580bf5edae39 # v8.2.0
        with:
          activate-environment: true
          enable-cache: false
          prune-cache: true
          python-version: ${{ env.TESTS_VERSION }}


      - name: Install pyaedt and tests dependencies
        env:
          TESTS_VERSION: ${{ env.TESTS_VERSION }}
        run: |
          uv sync --frozen --group tests --extra all


      - name: Remove Ansys processes (if any)
        shell: powershell
        run: |
          Get-Process | Where-Object {
            $_.Path -like "*ansys inc*" -or $_.Path -like "*ansysem*"
          } | ForEach-Object {
            Write-Output "Killing $($_.Name) (PID: $($_.Id))"
            Stop-Process -Id $_.Id -Force
          }

      - name: Run tests marked with 'emit'
        env:
          PYTHONMALLOC: malloc
          PYTEST_ARGUMENTS: ${{ env.PYTEST_ARGUMENTS }}
        run: |
          $args = $env:PYTEST_ARGUMENTS -split ' '
          pytest @args --timeout=600 -v -rA --color=yes tests/system/emit

      - name: Prepare testmon data for caching
        run: |
          python -c "import sqlite3; conn = sqlite3.connect('.testmondata'); conn.execute('PRAGMA journal_mode = OFF'); conn.close()"

      - name: Delete old testmon caches
        shell: bash
        run: |
          gh cache list --key ${CACHE_KEY}-main- --json key --jq '.[].key' | xargs -I {} gh cache delete {} || true
        env:
          CACHE_KEY: ${{ env.TESTMON_CACHE_KEY_EMIT_WIN }}
          GH_TOKEN: ${{ github.token }}

      - name: Save updated testmon cache
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ env.TESTMON_DATAFILE }}
          key: ${{ env.TESTMON_CACHE_KEY_EMIT_WIN }}-main-${{ github.sha }}
